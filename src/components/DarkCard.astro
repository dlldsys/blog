---
interface Props {
  class?: string;
}
const { class: className = '' } = Astro.props;
---

<div class={`p-6 ${className}`} style={`background: var(--c-code-bg); color: var(--c-code-text); border-radius: var(--r-card);`}>
  <slot />
</div>
