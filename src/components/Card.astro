---
interface Props {
  class?: string;
  as?: string;
}
const { class: className = '', as: Tag = 'div' } = Astro.props;
---

<Tag
  class={`rounded-xl border border-ink-black/[0.08] bg-pure-white p-6 transition-all duration-200 ease-out ${className}`}
>
  <slot />
</Tag>
